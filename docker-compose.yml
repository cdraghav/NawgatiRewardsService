version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: postgres_rewards_db
    restart: always
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"


# psql -U myuser -d mydatabase